<!DOCTYPE html>
<html lang="en">

<head>

    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>
            Login</title>
        <link rel="stylesheet" href="/css/main.css" />
    </head>
    <style>
        html, body {
            height: 100%; /* 전체 높이를 설정 */
            margin: 0; /* 기본 마진 제거 */
        }

        .hero-container {
            min-height: 100%; /* 최소 높이를 전체 화면으로 설정 */
            display: flex;
            flex-direction: column;
            justify-content: center; /* 세로 중앙 정렬 */
            align-items: center; /* 가로 중앙 정렬 */
        }
    </style>
<body class="bg-base-200 max-h-screen height:100%">

    <%- include('partials/navbar', { page: "/lego/login" }) %>

        <!--HERO-->
        <div class="hero bg-green-500 text-purple-700 p-10">
            <div class="hero-content text-center">
                <div class="max-w-md">
                    <h1 class="text-5xl font-bold">Log In</h1>
                    <p class="py-6 text-gray-700">Please provide your credentials below</p>
                    </p>

                    <div class="flex gap-4 justify-center"></div>

                    <form action="/login" method="POST" class="space-y-4">
                        <!-- User Name Field -->
                        <div>
                            <label for="userName" class=" block text-sm font-medium text-gray-700">User Name</label>
                            <input type="text" name="userName" id="userName" placeholder="User Name" required
                                class="input input-bordered w-full border-gray-300  focus:ring-2 focus:ring-blue-500 focus:outline-none"
                                value="<%= userName || '' %>">
                        </div>
                        <!-- Password Field -->
                        <div>
                            <label for="password" class="block text-sm font-medium text-gray-700">Password</label>
                            <input type="password" name="password" id="password" placeholder="Password" required
                                class="input input-bordered w-full border-gray-300 focus:ring-2 focus:ring-blue-500 focus:outline-none">
                        </div>
                        <!-- Submit Button -->
                         <br></br>
                        <div>
                            <button type="submit" class="btn btn-primary">
                                Login
                            </button>
                        </div>
                    </form>
                    <!-- Error Message -->
                    <% if (errorMessage) { %>
                        <div class="alert alert-error">
                            <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none"
                                viewBox="0 0 24
            24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-
            2a9 9 0 11-18 0 9 9 0 0118 0z" />
                            </svg>
                            <span>
                                <%= errorMessage %>
                            </span>
                        </div>
                        <% } %>
                </div>
            </div>
        </div>


</body>

</html>