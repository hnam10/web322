<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit LEGO Set - <%= set.name %>
  </title>
  <link rel="stylesheet" href="/css/main.css" />
</head>

<body class="bg-base-200">

  <!-- Navbar -->
  <%- include('partials/navbar', { page: "" }) %>

    <!-- Hero Section -->
    <div class="hero text-purple-700 p-10">
      <div class="hero-content text-center">
        <div class="max-w-md">
          <h1 class="text-5xl font-bold">Edit LEGO Set</h1>
          <p class="py-6">Update the details of the LEGO set below.</p>
        </div>
      </div>
    </div>

    <!-- Edit Set Form -->
    <div class="p-10">
      <form action="/lego/updateSet/<%= set.set_num %>" method="POST" class="form">
        <div class="form-group">
          <label for="set_name" class="label">Set Name:</label>
          <input type="text" id="set_name" name="name" value="<%= set.name %>" class="input input-bordered" required />
        </div>

        <div class="form-group mt-4">
          <label for="set_year" class="label">Year:</label>
          <input type="number" id="set_year" name="year" value="<%= set.year %>" class="input input-bordered"
            required />
        </div>

        <div class="form-group mt-4">
          <label for="set_num_parts" class="label">Number of Parts:</label>
          <input type="number" id="set_num_parts" name="num_parts" value="<%= set.num_parts %>"
            class="input input-bordered" required />
        </div>

        <div class="form-group mt-4">
          <label for="set_theme_id" class="label">Theme ID:</label>
          <input type="number" id="set_theme_id" name="theme_id" value="<%= set.theme_id %>"
            class="input input-bordered" required />
        </div>

        <div class="form-group mt-4">
          <label for="set_img_url" class="label">Image URL:</label>
          <input type="text" id="set_img_url" name="img_url" value="<%= set.img_url %>" class="input input-bordered" />
        </div>

        <!-- Update Set Button -->
        <div class="form-group mt-6">
          <button type="submit" class="btn btn-primary">Update Set</button>
        </div>
      </form>

      <!-- Delete Set Button (중복 제거 후 하나만 유지) -->
      <form action="/lego/deleteSet/<%= set.set_num %>" method="GET" class="mt-6">
        <button type="submit" class="btn btn-error">Delete Set</button>
      </form>
    </div>

    <!-- Back Button -->
    <div class="mt-10">
      <a href="/lego/sets" class="btn">Go Back to All Sets</a>
    </div>

</body>

</html>